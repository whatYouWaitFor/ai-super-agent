spring:
  ai:
    mcp:
      server:
        name: image-search-mcp-server
        version: 0.0.1
        type: sync
        # sse模式
        stdio: false
        #annotation-scanner:
        #  enabled: true
        #protocol: sse