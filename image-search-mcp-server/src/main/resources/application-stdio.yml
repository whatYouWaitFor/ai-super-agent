spring:
  ai:
    mcp:
      server:
        name: image-search-mcp-server
        version: 0.0.1
        type: SYNC
        # stdio
        stdio: true
        annotation-scanner:
          enabled: true

  # stdio
  main:
    # ??web??
    web-application-type: none
    banner-mode: off
